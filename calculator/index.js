// const { mathOperations, operation, numbers } = require("./lib");
const { mathOperations, operation, numbers} = require("./lib");

console.log("hello from danya");

const result = mathOperations(operation, numbers);
console.log(result);
